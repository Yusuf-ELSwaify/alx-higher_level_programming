#!/usr/bin/python3
"""defines a function to matrix multiplication using numpy."""
import numpy as np


def lazy_matrix_mul(m_a, m_b):
    """matrix multiplication using numpy."""
    return (np.matmul(m_a, m_b))
